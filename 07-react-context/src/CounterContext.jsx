import { createContext } from "react";

export const CounterContext = createContext(null); //initial value of the context is set as null
//createContext provides two things.
//1. Provider
//2. Consumer
